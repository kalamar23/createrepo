Create repo in Github from command line
========================

With this script you can create a new repository in github from command line, how do this ? the script use the API of Github.

The purpose ? if you feel more confortable with command line , the next script let you do things more fast without that you goes to web browser.
